# Copy to .env and fill in ONE of these. Neither is required -- with no key the
# pipeline runs deterministically, offline, end to end.

LLM_PROVIDER=groq
GROQ_API_KEY=

# LLM_PROVIDER=gemini
# GEMINI_API_KEY=
